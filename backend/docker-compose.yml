version: '3.8'

services:
  backend:
    build: .
    container_name: sdoc_backend
    ports:
      - "8000:8000"
    # Load local backend secrets and configuration
    env_file:
      - .env.local
    volumes:
      # Mount local inbox & attachments so updates reflect immediately
      - ./inbox:/app/inbox
      - ./attachments:/app/attachments
    restart: always