import { inboxRows } from "@/lib/server-data";
import Link from "next/link";
const labels: Record<string,string> = { unreadable:"Unreadable attachment", missing_attachment:"Missing attachment", wrong_doc_type:"Wrong document type", missing_value:"Missing source value" };
export const dynamic = "force-dynamic";
export default async function ReviewPage() { const rows = (await inboxRows()).filter(row => row.comparison?.status === "NEEDS_REVIEW"); const grouped = Object.groupBy(rows, row => row.comparison?.review_reason ?? "unknown"); return <section className="p-8"><p className="text-sm font-semibold text-[#0f6b66]">HUMAN IN THE LOOP</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Review queue</h1><p className="mt-2 text-slate-500">Oldest cases first. Correct data only where evidence supports it.</p>{rows.length === 0 ? <div className="mt-8 rounded-2xl border border-dashed border-emerald-300 bg-emerald-50 p-12 text-center"><h2 className="text-xl font-semibold text-emerald-800">Queue is clear</h2><p className="mt-2 text-emerald-700">Every current comparison was resolved automatically.</p></div> : <div className="mt-7 space-y-7">{Object.entries(grouped).map(([reason, items]) => <div key={reason}><h2 className="mb-3 text-sm font-bold uppercase tracking-wider text-slate-500">{labels[reason] ?? reason}</h2><div className="grid gap-3">{items?.map(row => <article key={row.id} className="flex items-center justify-between rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div><p className="font-semibold">{row.subject}</p><p className="mt-1 text-sm text-slate-500">{row.sender} · {row.id}</p></div><Link href={`/report/${row.id}`} className="rounded-lg bg-amber-100 px-3 py-2 text-sm font-semibold text-amber-800">Review case</Link></article>)}</div></div>)}</div>}</section>; }
