import { insights } from "@/lib/server-data";
export const dynamic = "force-dynamic";
function Bar({ label, value, total }: { label:string; value:number; total:number }) { return <div><div className="mb-1 flex justify-between text-sm"><span>{label.replaceAll("_", " ")}</span><b>{value}</b></div><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#0f6b66]" style={{width:`${Math.max(2, value / Math.max(1,total)*100)}%`}}/></div></div>; }
export default async function InsightsPage() { const data = await insights(); return <section className="p-8"><p className="text-sm font-semibold text-[#0f6b66]">OPERATIONS INTELLIGENCE</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Insights</h1><div className="mt-7 grid gap-4 md:grid-cols-4">{[["Total processed",data.total],["Automation rate",`${Math.round(data.automationRate*100)}%`],["Mismatch rate",data.comparisonCount?`${Math.round(data.mismatchCount/data.comparisonCount*100)}%`:"—"],["Human reviews",data.reviewCount]].map(([label,value])=><div key={String(label)} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-semibold">{value}</p></div>)}</div><div className="mt-6 grid gap-5 lg:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-semibold">Category breakdown</h2><div className="mt-5 space-y-4">{Object.entries(data.category).map(([label,value])=><Bar key={label} label={label} value={value} total={data.total}/>)}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-semibold">Review reasons</h2><div className="mt-5 space-y-4">{Object.entries(data.reasons).length ? Object.entries(data.reasons).map(([label,value])=><Bar key={label} label={label} value={value} total={data.reviewCount}/>) : <p className="text-sm text-slate-500">No cases currently need review.</p>}</div><p className="mt-6 text-xs text-slate-400">Automation rate = 1 − cases needing review / all comparisons.</p></div></div></section>; }
