import Link from "next/link"; import { createAdminClient } from "@/lib/supabase/admin";
export const dynamic="force-dynamic";
export default async function Admin(){const db=createAdminClient();const {data}=await db.from("audit_log").select("email_id,actor,action,created_at").order("created_at",{ascending:false}).limit(30);return <section className="p-8"><p className="text-sm font-semibold text-[#0f6b66]">ADMINISTRATION</p><h1 className="mt-1 text-3xl font-semibold">Export and audit trail</h1><div className="mt-7 flex gap-3"><Link href="/api/export/submission" className="rounded-lg bg-[#0f6b66] px-4 py-3 text-sm font-semibold text-white">Download submission.json</Link><span className="rounded-lg border border-slate-200 bg-white px-4 py-3 text-sm text-slate-500">Run self-eval locally with organiser server</span></div><div className="mt-7 overflow-hidden rounded-xl border border-slate-200 bg-white"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-xs text-slate-500"><tr><th className="p-4">Time</th><th className="p-4">Email</th><th className="p-4">Actor</th><th className="p-4">Action</th></tr></thead><tbody>{data?.map((x,i)=><tr key={i} className="border-t"><td className="p-4 text-slate-500">{new Date(x.created_at).toLocaleString()}</td><td className="p-4">{x.email_id}</td><td className="p-4">{x.actor}</td><td className="p-4">{x.action}</td></tr>)}</tbody></table></div></section>}
